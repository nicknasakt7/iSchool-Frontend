// This hook has been replaced by useAssessmentConfig which uses the correct
// backend query params (classroomId, subjectId, term, year).
// Import useAssessmentConfig instead.
export { useAssessmentConfig as useStudentAssessments } from './useAssessmentConfig';
